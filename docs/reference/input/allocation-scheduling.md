<!--
  DO NOT EDIT. This file is generated by `./gradlew generateDocs`.
  Field names, types and columns come from the source code; edit prose in
  docs/descriptions/*.json and regenerate.
-->

# Allocation Policies

## AllocationPolicySpec { #allocationpolicyspec }

Specification describing how and when tasks are allocated to hosts by the scheduler. An allocation policy is selected in a JSON file by adding its `SerialName` as the `type`. The following implementations are available: - `PrefabAllocationPolicySpec` (`"prefab"`): one of the schedulers already present in OpenDC. - `FilterAllocationPolicySpec` (`"filter"`): a filter scheduler built from host filters and weighers. - `TimeShiftAllocationPolicySpec` (`"timeshift"`): a carbon-aware scheduler that shifts tasks in time.

Polymorphic type. Set the `type` field to select one of the following variants:

- `filter` → [AllocationPolicySpec.filter](#allocationpolicyspec-filter)
- `prefab` → [AllocationPolicySpec.prefab](#allocationpolicyspec-prefab)
- `timeshift` → [AllocationPolicySpec.timeshift](#allocationpolicyspec-timeshift)

### AllocationPolicySpec.prefab { #allocationpolicyspec-prefab }

Variant of [AllocationPolicySpec](#allocationpolicyspec), selected by setting `type` to `"prefab"`.

An allocation policy that uses one of the schedulers already built into OpenDC. This is the simplest way to configure scheduling: instead of assembling filters and weighers by hand, a ready-made scheduler is selected by name.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `policyName` | [ComputeSchedulerEnum](#computeschedulerenum) | no | The prefab scheduler to use. See `ComputeSchedulerEnum` for the available options. Default is `ComputeSchedulerEnum.Mem`. |
| `name` | String | no |  |

### AllocationPolicySpec.filter { #allocationpolicyspec-filter }

Variant of [AllocationPolicySpec](#allocationpolicyspec), selected by setting `type` to `"filter"`.

An allocation policy backed by a filter scheduler. When a task is scheduled the hosts are first reduced to the eligible ones by applying every `HostFilterSpec` in `filters`, and the remaining hosts are ranked by the combined score of every `HostWeigherSpec` in `weighers`. The task is then placed on one of the `subsetSize` highest-ranked hosts, picked at random.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `filters` | List&lt;[HostFilterSpec](#hostfilterspec)&gt; | no | Filters that remove hosts unable to run a task. Default is a single `ComputeFilterSpec`. |
| `weighers` | List&lt;[HostWeigherSpec](#hostweigherspec)&gt; | no | Weighers that rank the hosts that pass the filters. Default is no weighers. |
| `subsetSize` | Int | no | Number of top-ranked hosts to randomly pick the placement from. Must be positive. Default is 1, which always selects the single best host. |

### AllocationPolicySpec.timeshift { #allocationpolicyspec-timeshift }

Variant of [AllocationPolicySpec](#allocationpolicyspec), selected by setting `type` to `"timeshift"`.

A carbon-aware allocation policy that defers tasks to periods of lower carbon intensity. Like `FilterAllocationPolicySpec` it filters and weighs hosts, but it additionally decides whether the current moment is a "low carbon" regime and holds deferrable tasks until such a moment. The current carbon intensity is compared either against a quantile of the forecast (when `forecast` is true) or against the moving average of the past `windowSize` samples (when it is false). Separate short- and long-task regimes are tracked using `shortForecastThreshold` and `longForecastThreshold`.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `filters` | List&lt;[HostFilterSpec](#hostfilterspec)&gt; | no | Filters that remove hosts unable to run a task. Default is a single `ComputeFilterSpec`. |
| `weighers` | List&lt;[HostWeigherSpec](#hostweigherspec)&gt; | no | Weighers that rank the hosts that pass the filters. Default is no weighers. |
| `windowSize` | Int | no | Number of past carbon-intensity samples kept, and the length of the moving average used when `forecast` is false. Must be positive. Default is 168 (one week of hourly samples). |
| `subsetSize` | Int | no | Number of top-ranked hosts to randomly pick the placement from. Must be positive. Default is 1. |
| `forecast` | Boolean | no | Whether a forecast of the carbon intensity is used instead of the moving average. Default is true. |
| `shortForecastThreshold` | Double | no | Quantile, between 0.0 and 1.0, of the forecast below which the current intensity counts as low carbon for short tasks (shorter than 2 hours). Default is 0.2. |
| `longForecastThreshold` | Double | no | Quantile, between 0.0 and 1.0, of the forecast below which the current intensity counts as low carbon for long tasks. Default is 0.35. |
| `forecastSize` | Int | no | Number of samples forecasted into the future when `forecast` is true. Must be positive. Default is 24. |
| `taskStopper` | [taskstopper](experiment.md#taskstopper)? | no | Optional `TaskStopperSpec` that pauses already running tasks when the carbon intensity is high. Default is none. |
| `memorize` | Boolean | no | Whether to use the memorizing variant of the scheduler, which caches host rankings for speed. Default is true. |

## HostFilterEnum { #hostfilterenum }

One of:

- `Compute`
- `DifferentHost`
- `InstanceCount`
- `Ram`
- `SameHost`
- `VCpuCapacity`
- `VCpu`

## HostFilterSpec { #hostfilterspec }

A specification for a host filter. A host filter decides how a host is selected for a given allocation request. The filter is applied to all hosts in the data center and only those that pass the filter are considered for allocation. A host filter can be defined in a JSON object by adding the serialName as the type parameter. The user then has to specify any additional parameters required for the filter. The following filters are available: `ComputeFilterSpec` (`"Compute"`), `SameHostHostFilterSpec` (`"SameHost"`), `DifferentHostFilterSpec` (`"DifferentHost"`), `InstanceCountHostFilterSpec` (`"InstanceCount"`), `RamHostFilterSpec` (`"Ram"`), `VCpuCapacityHostFilterSpec` (`"VCpuCapacity"`) and `VCpuHostFilterSpec` (`"VCpu"`).

Polymorphic type. Set the `type` field to select one of the following variants:

- `Compute` → [HostFilterSpec.Compute](#hostfilterspec-compute)
- `DifferentHost` → [HostFilterSpec.DifferentHost](#hostfilterspec-differenthost)
- `InstanceCount` → [HostFilterSpec.InstanceCount](#hostfilterspec-instancecount)
- `Ram` → [HostFilterSpec.Ram](#hostfilterspec-ram)
- `SameHost` → [HostFilterSpec.SameHost](#hostfilterspec-samehost)
- `VCpuCapacity` → [HostFilterSpec.VCpuCapacity](#hostfilterspec-vcpucapacity)
- `VCpu` → [HostFilterSpec.VCpu](#hostfilterspec-vcpu)

### HostFilterSpec.Compute { #hostfilterspec-compute }

Variant of [HostFilterSpec](#hostfilterspec), selected by setting `type` to `"Compute"`.

Filter that only keeps hosts that are currently up and running. A host that is down (for example due to a failure) can never run a task, so this basic filter is almost always included in a policy.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `filterName` | [HostFilterEnum](#hostfilterenum) | no | Discriminator identifying this filter. Always `HostFilterEnum.Compute`. |

### HostFilterSpec.SameHost { #hostfilterspec-samehost }

Variant of [HostFilterSpec](#hostfilterspec), selected by setting `type` to `"SameHost"`.

Affinity filter that keeps only the hosts a task wants to be co-located with. Note: host affinity hints are currently not applied, so this filter presently keeps every host.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `filterName` | [HostFilterEnum](#hostfilterenum) | no | Discriminator identifying this filter. Always `HostFilterEnum.SameHost`. |

### HostFilterSpec.DifferentHost { #hostfilterspec-differenthost }

Variant of [HostFilterSpec](#hostfilterspec), selected by setting `type` to `"DifferentHost"`.

Anti-affinity filter that removes the hosts a task wants to be kept apart from. Note: host anti-affinity hints are currently not applied, so this filter presently keeps every host.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `filterName` | [HostFilterEnum](#hostfilterenum) | no | Discriminator identifying this filter. Always `HostFilterEnum.DifferentHost`. |

### HostFilterSpec.InstanceCount { #hostfilterspec-instancecount }

Variant of [HostFilterSpec](#hostfilterspec), selected by setting `type` to `"InstanceCount"`.

Filter that keeps only hosts running fewer than `limit` tasks, capping how many tasks share a host.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `filterName` | [HostFilterEnum](#hostfilterenum) | no | Discriminator identifying this filter. Always `HostFilterEnum.InstanceCount`. |
| `limit` | Int | **yes** | Maximum number of tasks allowed on a host; only hosts below this count pass. Must be positive. |

### HostFilterSpec.Ram { #hostfilterspec-ram }

Variant of [HostFilterSpec](#hostfilterspec), selected by setting `type` to `"Ram"`.

Filter that keeps only hosts with enough memory available to run the task. With an `allocationRatio` of 1.0 a host must have at least as much free memory as the task requests. A higher ratio permits memory overcommit: up to `memoryCapacity * allocationRatio` may be handed out across tasks, though a task can never overcommit against itself.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `filterName` | [HostFilterEnum](#hostfilterenum) | no | Discriminator identifying this filter. Always `HostFilterEnum.Ram`. |
| `allocationRatio` | Double | no | Memory overcommit ratio. Must be positive. Default is 1.0 (no overcommit). |

### HostFilterSpec.VCpuCapacity { #hostfilterspec-vcpucapacity }

Variant of [HostFilterSpec](#hostfilterspec), selected by setting `type` to `"VCpuCapacity"`.

Filter that keeps only hosts whose per-core CPU capacity meets the task's per-core CPU demand.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `filterName` | [HostFilterEnum](#hostfilterenum) | no | Discriminator identifying this filter. Always `HostFilterEnum.VCpuCapacity`. |

### HostFilterSpec.VCpu { #hostfilterspec-vcpu }

Variant of [HostFilterSpec](#hostfilterspec), selected by setting `type` to `"VCpu"`.

Filter that keeps only hosts with enough free vCPU cores to run the task. With an `allocationRatio` of 1.0 a host must have at least as many free cores as the task requests. A higher ratio permits CPU overcommit: up to `coreCount * allocationRatio` cores may be handed out across tasks, though a task can never overcommit against itself.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `filterName` | [HostFilterEnum](#hostfilterenum) | no | Discriminator identifying this filter. Always `HostFilterEnum.VCpu`. |
| `allocationRatio` | Double | no | vCPU overcommit ratio. Must be positive. Default is 1.0 (no overcommit). |

## HostWeigherSpec { #hostweigherspec }

A specification for a host weigher. A host weigher can be defined in a JSON file by adding the serialName as the type parameter. The user then has to specify any additional parameters required for the weigher. Every weigher is scaled by a `multiplier`, which controls how strongly it contributes to the final host ranking. The following weighers are available: `RamWeigherSpec` (`"Ram"`), `CoreRamWeigherSpec` (`"CoreRam"`), `InstanceCountWeigherSpec` (`"InstanceCount"`), `VCpuCapacityWeigherSpec` (`"VCpuCapacity"`) and `VCpuWeigherSpec` (`"VCpu"`).

Polymorphic type. Set the `type` field to select one of the following variants:

- `CoreRam` → [HostWeigherSpec.CoreRam](#hostweigherspec-coreram)
- `InstanceCount` → [HostWeigherSpec.InstanceCount](#hostweigherspec-instancecount)
- `Ram` → [HostWeigherSpec.Ram](#hostweigherspec-ram)
- `VCpuCapacity` → [HostWeigherSpec.VCpuCapacity](#hostweigherspec-vcpucapacity)
- `VCpu` → [HostWeigherSpec.VCpu](#hostweigherspec-vcpu)

### HostWeigherSpec.Ram { #hostweigherspec-ram }

Variant of [HostWeigherSpec](#hostweigherspec), selected by setting `type` to `"Ram"`.

Weigher that ranks hosts by the amount of memory they have available, favouring emptier hosts.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `multiplier` | Double | no | Factor by which this weigher is scaled when combined with other weighers. Default is 1.0. |

### HostWeigherSpec.CoreRam { #hostweigherspec-coreram }

Variant of [HostWeigherSpec](#hostweigherspec), selected by setting `type` to `"CoreRam"`.

Weigher that ranks hosts by their available memory per CPU core.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `multiplier` | Double | no | Factor by which this weigher is scaled when combined with other weighers. Default is 1.0. |

### HostWeigherSpec.InstanceCount { #hostweigherspec-instancecount }

Variant of [HostWeigherSpec](#hostweigherspec), selected by setting `type` to `"InstanceCount"`.

Weigher that ranks hosts by the number of tasks already running on them.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `multiplier` | Double | no | Factor by which this weigher is scaled when combined with other weighers. Default is 1.0. |

### HostWeigherSpec.VCpuCapacity { #hostweigherspec-vcpucapacity }

Variant of [HostWeigherSpec](#hostweigherspec), selected by setting `type` to `"VCpuCapacity"`.

Weigher that ranks hosts by their spare CPU capacity per core.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `multiplier` | Double | no | Factor by which this weigher is scaled when combined with other weighers. Default is 1.0. |

### HostWeigherSpec.VCpu { #hostweigherspec-vcpu }

Variant of [HostWeigherSpec](#hostweigherspec), selected by setting `type` to `"VCpu"`.

Weigher that ranks hosts by their available vCPU cores.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `multiplier` | Double | no | Factor by which this weigher is scaled when combined with other weighers. Default is 1.0. |

## ComputeSchedulerEnum { #computeschedulerenum }

One of:

- `Mem`
- `MemInv`
- `CoreMem`
- `CoreMemInv`
- `ActiveServers`
- `ActiveServersInv`
- `ProvisionedCores`
- `ProvisionedCoresInv`
- `Random`
- `TaskNumMemorizing`
- `Timeshift`
- `ProvisionedCpuGpuCores`
- `ProvisionedCpuGpuCoresInv`
- `GpuTaskMemorizing`

